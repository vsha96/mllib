module Mllib.Utils.ModelSelection
    (

    ) where


-- | TODO
-- -- split data function (train val test) [sklearn.model_selection -> train_test_split]
-- -- make the same above n times [sklearn.model_selection -> ShuffleSplit]
-- -- split data accord. to groups [sklearn.model_selection -> GroupShuffleSplit]
-- -- split data and save proportions of classes [sklearn.model_selection -> StratifiedShuffleSplit]
-- -- bootstrap (like Bernoulli scheme)
-- -- k-fold cross-validation [sklearn.model_selection -> KFold]
-- -- LOOCV (k-fold, k = number of objects)
-- -- ...

-- | TODO
-- -- data split for time series [sklearn.model_selection -> TimeSeriesSplit]


